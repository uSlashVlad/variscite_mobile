import 'package:flutter/material.dart';

class VarisciteName extends StatelessWidget {
  const VarisciteName({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Variscite Mobile',
      style: TextStyle(
        fontWeight: FontWeight.bold,
        fontSize: 20,
      ),
    );
  }
}
